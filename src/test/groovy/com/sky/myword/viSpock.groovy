package com.sky.myword

import com.sky.myword.test1116.Cursor
import spock.lang.Specification

class viSpock  extends Specification{
  /*  def "光标处 插入 liukaili-1 文本开始处插入"(){
        given:
        TemplateDAO dao = Mock()
        dao.getTemplateByName(_) >> { String templateName ->
            NewDocument document=new NewDocument()
            document.content=templateName
            return document
        }
        def factory = new DocumentFactoryTemplateImpl(dao)
        def document = factory.createByTemplate("你好")

        def cursor=new Cursor()
        char[] chars="liukaili".toCharArray()
        document.insertChars(cursor,chars)
        expect:
        document.content=="liukaili你好"

    }




    def "光标处 插入 liukaili-2-文本中间"(){
        given:
        TemplateDAO dao = Mock()
        dao.getTemplateByName(_) >> { String templateName ->
            NewDocument document=new NewDocument()
            document.content=templateName
            return document
        }
        def factory = new DocumentFactoryTemplateImpl(dao)
        def document = factory.createByTemplate("你好")

        def cursor=new Cursor()
        cursor.x=2
        char[] chars="liukaili".toCharArray()
        document.insertChars(cursor,chars)
        expect:
        document.content=="你好liukaili"

    }


    def "光标处 插入 liukaili-3-插入位置超出文本内容"(){
        given:
        TemplateDAO dao = Mock()
        dao.getTemplateByName(_) >> { String templateName ->
            NewDocument document=new NewDocument()
            document.content=templateName
            return document
        }
        def factory = new DocumentFactoryTemplateImpl(dao)
        def document = factory.createByTemplate("你好")

        def cursor=new Cursor()
        cursor.x=2
        char[] chars="liukaili".toCharArray()
        document.insertChars(cursor,chars)
        expect:
        document.content=="你好liukaili"

    }*/
}
